# INDEX de Arquitetura — MapasCulturais

> **Classe REFERENCE** — única versão, editada in place. Documento vivo.
> **Como usar**: encontre sua necessidade na tabela, leia os arquivos NA ORDEM da coluna 3. Toda âncora é `arquivo:linha` real do repo (linhas estáveis na data de criação; quando o arquivo evolui rápido, cite por símbolo).
> **Convenções verificadas**: views são **PHP** (não Mustache — Mustache é só para e-mails); hooks são case-insensitive; a suíte de testes roda por `bash tests/run.sh`; schema evolui por 168 updates nomeados (db-updates), sem migrations versionadas.

| # | Você precisa de... | Ler nesta ordem | Quando ler |
|---|---|---|---|
| 1 | **Visão geral em 30 min** | `README.md` → `src/bootstrap.php` (constantes de caminho, 11-25) → `src/core/App.php:453-531` (`App::init`, ordem exata de inicialização) → `docs/reference/architecture.md` §2 (Bootstrap) → ADR `docs/reference/decisions/0006-evolucao-schema-por-ledger-db-updates.md` | Primeiro dia |
| 2 | **(a) Prever qual controller/método atende uma URL e o que renderiza** | `config/routes.php` (shortcuts/aliases; nota: `'inscricao'` triplicado — última chave vence) → `docs/reference/architecture.md` §3 (Resolução de rota) → `src/core/RoutesManager.php:114-214` (catch-all → `replaceShortcuts` → `extractArgs`) → `src/core/Controller.php:276-353` (`callAction`: `METHOD_action` → `ALL_action` → hooks `GET(id.action)`; nunca `ALL_` para API) → `src/core/Traits/ControllerEntityViews.php` (`GET_index/single/edit`) → `src/core/App.php:3137-3230` (registro de controllers core) + `src/modules/Panel/Module.php:26` (panel sob BaseV2 vem do módulo, não do core) → views: hierarquia de paths em `src/core/Theme.php:742-754` (`resolveFilename`: tema ativo filho→pai → plugins → módulos) → evidência executável: `tests/src/RoutesTest.php` (matriz 401/403/200) → skill `.agents/skills/mapas-culturais-controllers/` | Antes de criar/alterar rota, action ou URL; ao debugar 404/403 |
| 3 | **(b) Campo customizado: registro → persistência → API** | `src/core/App.php:4946-4993` (`registerMetadata`) → os **5 canais de registro** (`src/conf/agent-types.php` como exemplo; runtime por tema/módulo; `Opportunity::registerRegistrationMetadata` para formulários de inscrição, `src/core/Entities/Opportunity.php:1648-1767`) → `src/core/Definitions/Metadata.php:167-296` (contrato, serialize/unserialize por tipo) → `src/core/Traits/EntityMetadata.php:86-154,320-362` (magic get/set; **throw em chave não registrada, linha 330**) → tabela `{x}_meta` (DDL: 11 tabelas dedicadas; `metadata` polimórfica é vestigial — 0 usos no core) → API: `src/core/Entity.php:1370-1381` (`jsonSerialize`) + `src/core/ApiQuery.php:3936-3965` (filtro EAV; `IN`→`JSON_IN` automático p/ multiselect) → ADRs `decisions/0002-metadados-eav-por-entidade.md` e `decisions/0008-metadata-eav-por-entidade-lado-dados.md` → skill `.agents/skills/mapas-culturais-metadata-eav/` → evidência: `tests/src/EntityMetadataTest.php` | Antes de adicionar campo; quando campo "não salva" ou "não aparece na API" |
| 4 | **(c) Ciclo completo de uma inscrição** | `docs/reference/architecture.md` §14 (Domínio: avaliação, distribuição e fases) → `src/core/Entities/Registration.php:54-58,1060-1074,1179-1294` (status, `_setStatusTo` com guarda de permissão, `send` com snapshot `agentsData`) → `src/core/Controllers/Registration.php:526-546` (guarda `'First status change should be pending'`), `:611` (`POST_send`), `:526,566` (setStatusTo/setMultipleStatus) → validações de envio: `Registration.php:1437-1643` + `src/modules/RegistrationFieldTypes/Module.php` (hooks `sendValidationErrors`) → fases e jobs: `src/modules/Opportunities/Module.php:553-604` (enqueue por datas) + `src/modules/Opportunities/Jobs/` (8 JobTypes) + `src/modules/OpportunityPhases/Module.php:1303-1767` (sync inter-fases, `number` como chave lógica, ponteiros `previous/nextPhaseRegistrationId`) → distribuição: `src/core/EvaluationMethod.php:785-1338` → publicação: `src/core/Entities/Opportunity.php:1103-1171` + job `PublishResult` → ADRs `decisions/0007-fila-de-jobs-no-postgresql.md`, `0009-metodos-de-avaliacao-plugaveis.md`, `0010-fases-como-oportunidades-filhas-sincronizadas-por-numero.md`, `0011-workplan-na-inscricao-e-monitoramento-reporting-phase.md` → skill `.agents/skills/mapas-culturais-status-lifecycle/` → evidência: `tests/src/EvaluationConsolidationTest.php`, `tests/src/PhaseRegistrationSyncTest.php` | Antes de mexer em inscrições, avaliações, prazos, resultado, fases |
| 5 | **(d) Armadilhas do sistema de hooks** | `src/core/Hooks.php` (inteiro; `_compile:320-344` — wildcard `*` só dentro de `<<...>>`; case-insensitive `#i`) → ADR `decisions/0001-hooks-regex-com-wildcards-e-binding.md` (catálogo de armadilhas) → skill `.agents/skills/mapas-culturais-hooks-dsl/` → docblocks `src/core/Controller.php:42-57` e `src/core/Entity.php:29-56` → lista canônica de lifecycle hooks e armadilhas (`save:after` é **pré-flush**; `save:finish` com `flush=false` dispara sem SQL; `get()` hook é opt-in por entidade; `unarchive:after` morto por bug) → exemplos: `src/core/Theme.php:233`, `src/modules/RegistrationFieldTypes/Module.php:601,659` → evidência: `tests/src/HooksTest.php` (`testHookOrder`, `testHookWildcard`) | Antes de escrever QUALQUER listener; quando um hook "não dispara" |
| 6 | **(d) Permissões, pcache e workflow requests** | `docs/reference/architecture.md` §6 (Permissões e pcache) → `src/core/Entity.php:475-760` (`canUser*`, `checkPermission`; hooks `can(...)` mutáveis) → pcache em 2 camadas: `src/core/Traits/EntityPermissionCache.php:59-160` (INSERT `ON CONFLICT DO NOTHING`) + fila `src/core/App.php:2536-2700` (`persistPCachePendingQueue`, `recreatePermissionsCache`) + consumidor `docker/recreate-pending-pcache-cron.sh` → workflow: `src/core/Entities/Request.php:202-299` (`approve/reject`; `@control` no destination) + `src/core/Entity.php:1204-1275` (coleta `save:requests`; `PermissionDenied` engolida se há requests → HTTP 202 em `RoutesManager.php:99-104`) → 401 (`requireAuthentication`) vs 403 (`PermissionDenied`) — `src/core/Controller.php:455-463`, `src/core/AuthProvider.php:128-147`, `RoutesManager.php:94-97` → ADR `decisions/0003-permission-cache-materializada-com-fila-assincrona.md` → skills `.agents/skills/mapas-culturais-permissions-pcache/` e `.agents/skills/mapas-culturais-route-permissions/` → `documentation/docs/mc_permission_cache.md` (legada) | Antes de checar permissão em código; quando usuário "não vê" entidade; 202 inesperado |
| 7 | **(e) Comandos de build/teste/deploy** | `AGENTS.md` (raiz do repo — lista verificada) → `dev/docker-compose.yml` + `dev/start.sh` (fluxo dev: `./start.sh [-b]`) → `docker/Dockerfile` → `docker/entrypoint.sh` (boot = deploy: wait DB → db-update 2 passadas → mc-db-updates → sass/proxies se `version.txt` mudou → 3 crons) → `tests/run.sh` + `tests/docker-compose.yml` → `.github/workflows/ci.yml` (build de imagem; **CI não roda testes**) → `config/0.main.php` (envs) → ADRs `decisions/0015-container-unico-web-workers.md`, `0016-ci-build-only-sem-gate-de-testes.md`, `0017-deploy-imagem-docker-migrations-no-boot.md` | Setup local, publicar imagem, deploy de instância |
| 8 | **Jobs/fila (agendar, debugar)** | `docs/reference/architecture.md` §8 (Jobs) → `src/core/App.php:2253-2522` (enqueue com id determinístico deduplicador; claim atômico `UPDATE...RETURNING`) → `src/core/Entities/Job.php:201-247` (sucesso deleta/reagenda; **exceção = zombie status=1 sem retry**; recuperação só para `iterations==1` após 5 min) → `docker/jobs-cron.sh` (loop `NUM_PROCESSES`) → ADR `decisions/0007-fila-de-jobs-no-postgresql.md` → ver RB2 | Trabalho assíncrono; fila parada; resultado não publica |
| 9 | **Migrations (db-update / mc-update)** | `src/db-updates.php` (168 updates) + `src/mc-updates.php` (30) → `src/tools/apply-updates.php` → `scripts/db-update.sh:24-28` (2 passadas: `DISABLE_PLUGINS=1` → com plugins) → semânticas `__exec` (falha → re-executa) × `__try` (grava mesmo falhando) × `return false` (sempre roda) em `src/core/App.php:5443-5488` — **exceção não aborta o boot** → updates de módulo/tema entram pela varredura de `view->path` (`App.php:5455-5460`) → ADR `decisions/0006-evolucao-schema-por-ledger-db-updates.md` → skill `.agents/skills/mapas-culturais-db-updates-migrations/` → ver RB3 + runbook-receita | Qualquer mudança de schema ou backfill de dados |
| 10 | **Uploads/arquivos (grupos, privacidade)** | `src/core/Traits/ControllerUploads.php:52-200` (pipeline com 4 gates; **grupo não registrado = silêncio**) → `src/core/Entities/File.php:234-255` (decisão de `private`: grupo primeiro, status do owner como fallback) → `src/core/Storage/FileSystem.php` (paths `{entidade}/{ownerId}/...`; privado fisicamente fora do docroot) → `src/core/App.php:3307-3509` (catálogo de file groups) → anexos de inscrição: grupos dinâmicos `rfc_{id}` em `src/core/Controllers/Registration.php:30-130` (sempre private; renomeação rastreável) → servimento: `Controllers/File.php:37-77` (`privateFile` com `checkPermission('view')`) | Upload novo, anexo que "some", permissão de arquivo |
| 11 | **API query DSL (ApiQuery)** | `docs/reference/architecture.md` §9 (ApiQuery) → `src/core/API.php` (fábrica de expressões) → `src/core/ApiQuery.php:3683-3748` (`parseQueryParams`; chave desconhecida → `PropertyDoesNotExists`) → `:3443-3616` (operadores com tradução DQL exata) → `:3830-3883` (subquery de pcache; 2 formatos) → `src/core/Traits/ControllerAPI.php:42-138` (hooks `API.*`; header `API-Metadata`) → armadilhas: **filtro por relação não-owning é aceito e ignorado** (`:3917-3925` `@TODO`); metadado numérico ordena como texto sem `CAST` (`:1558-1617`); `LIKE` é case-sensitive (`:3538`) → exemplos executáveis: `tests/src/ApiTest.php`, `OpportunityApiTest.php` → ADR `decisions/0004-apiquery-dsl-unica-de-consulta.md` → skill `.agents/skills/mapas-culturais-apiquery-dsl/` | Integrar/exportar dados; query que "retorna tudo" ou ordena errado |
| 12 | **Recorrência de eventos (agenda)** | `docs/reference/architecture.md` §11 (Recorrência) → `src/core/Entities/EventOccurrence.php:309-404` (`setRule`: regra JSON → colunas + `event_occurrence_recurrence`) → **procedure PL/pgSQL** `recurring_event_occurrence_for` (`dev/db/dump.sql:475-595` — existe SÓ nos dumps, nunca em db-updates; lacuna estrutural) → `src/core/Controllers/Event.php:384-626` (`apiOccurrences`: expansão no banco + correlação `space:`/`event:` + paginação em PHP) → armadilhas: editar regra reescreve o passado (nada é materializado); cache de agenda só por TTL; `monthly`-por-semana com `TODO: calc week` | Agenda de eventos; ocorrência "sumindo" ou repetida |
| 13 | **Subsites (multi-tenant por URL)** | `docs/reference/architecture.md` §7 (Auth/sessão/subsite) → `src/core/App.php:1017-1054` (`_initSubsite` por `HTTP_HOST`) → `:1099-1130` (tema e cache namespace por subsite) → `src/core/Entities/Subsite.php:259-404` (`applyApiFilters` = ApiQueries penduradas por hook; `applyConfigurations`) → jobs reconstroem o contexto do tenant (`App.php:2474-2498`) → ADR `decisions/0005-multi-tenant-por-dominio-com-filtros-injetados.md` | Instalar/configurar tenant; conteúdo "vazando" entre tenants |
| 14 | **Frontend: componentes Vue (padrão do projeto)** | `docs/reference/architecture.md` §13 (Frontend/SDK) → `src/modules/Components/Module.php:206-243` (`$this->import` → `$TEMPLATES`) → componente modelo: `src/modules/Components/components/mc-tag-list/` (README + script.js + texts.php) → regra: `script.js` cru com `app.component(...)`, **sem import/export/SFC** → CSS default em `src/themes/BaseV2/assets-src/sass/2.components/` (ITCSS/BEM) → ADR `decisions/0013-coexistencia-duas-geracoes-frontend-basev1-basev2.md` → skill `.agents/skills/mapas-culturais-vue-component-build/` | Criar/alterar componente; componente que "não renderiza" |
| 15 | **Frontend: SDK JS (Entity/API)** | `src/modules/Components/assets/js/components-base/API.js` (singleton `{type}:{scope}`; `persistEntity`; header `MAPAS-Force-Save`) → `Entity.js` (`populate`, `data(true)` diff mínimo, **save debounced com agregação de promises**) → `Utils.js` (`createUrl` = espelho JS do roteador; `parsePseudoQuery`) → `global-state.js` (user como Entity) → ponte PHP↔JS: `src/core/Theme.php:700-719` (`printJsObject` → `Mapas.*`) e `:889-1092` (`requestedEntity`) | Consumir/salvar entidades no front; comportamento estranho de save/cache |
| 16 | **Assets (enqueue/publicação)** | `docs/reference/architecture.md` §12 (AssetManager) → `src/core/Theme.php:647-663` (`enqueueScript/Style`) → `src/core/AssetManager.php:50-167` (ordenção por dependências; **dependência ausente = página 500**) → `src/core/AssetManagers/FileSystem.php` (merge+terser/uglifycss em RUNTIME na 1ª exibição; hash por mtime) → **grupos × geração**: `app`/`vendor` só BaseV1; `vendor-v2`/`app-v2`/`components` só BaseV2 (grupo errado = asset morto) → build: `src/node_scripts/webpack.mix.js` (só `assets-src/` de 2 pacotes) → ADR `decisions/0014-pipeline-duplo-de-assets-webpack-buildtime-assetmanager-runtime.md` → higiene: `src/tools/cleanup-orphan-assets.php` (**exige Redis**) → ver RB6 | Página sem CSS/JS; novo asset; deploy de tema |
| 17 | **Testes (escrever/rodar)** | `tests/run.sh` → `tests/src/Abstract/TestCase.php` (transação+rollback; **`processJobs`/`processPCache` obrigatórios** para jobs/permissões) → `tests/src/Abstract/Builder.php` + `Directors/` (fluent: `reset()->fillRequiredProperties()->save()`) → exemplo canônico `tests/src/EvaluationConsolidationTest.php` → **não existe** `composer test`/`phpunit.xml`/Makefile; CI não roda testes → arquivo único: `bash tests/bash.sh` → dentro do container `pu <path>` → skill `.agents/skills/mapas-culturais-tdd-builders/` | Antes de escrever qualquer teste |
| 18 | **Módulos (construir)** | `src/core/Module.php` (`_init()` + `register()`; path de views entra no tema) → modelos simples: `src/modules/FAQ/`, `src/modules/LGPD/` → registro de controller em `Module::register()` (ex.: `Opportunities/Module.php:1319-1326` — registro condicional) → config em `config/module.{Nome}.php` → skills `.agents/skills/mapas-culturais-module-build/` e `.agents/skills/mapas-culturais-controllers/` | Feature nova; entender onde um hook/rota nasce |
| 19 | **Entidades: registro e lifecycle completo** | `docs/reference/architecture.md` §4 (Entidades e ciclo de vida) → Catálogo de entidades e classes-irmãs: `src/core/Entities/` (padrão `X`/`XMeta`/`XFile`/`XPermissionCache`) → registro = convergência de convenções (annotations + `registerController` + `{X}Meta`) em `src/core/Controllers/EntityController.php:34` → lifecycle canônico: `src/core/Entity.php:1189-1285` (`save`), `:1587-1721` (callbacks Doctrine) → **`em->persist()` cru pula TODO o ciclo de domínio** (sem revisão/pcache/`save:*`) | Entidade nova; comportamento faltando ao salvar |
| 20 | **Fases de acompanhamento/workplan (o que está VIVO)** | VIVO = `ProjectMonitoring` (`isReportingPhase`/`isFinalReportingPhase`): `src/modules/ProjectMonitoring/Module.php` + Controller `POST reportingPhase` → workplan: `src/modules/OpportunityWorkplan/` (entidades `Workplan/Goal/Delivery`; ponte `workplanProxy`) → **MORTO (remoção decidida pelo dono)**: `OpportunityAccountability` (`Module.php:38` retorna cedo) — citar apenas como removido | Prestação de contas/monitoramento; "onde foi parar o accountability" |

**ADR** (todos em `docs/reference/decisions/`): `0001` hooks · `0002`+`0008` EAV · `0003` pcache · `0004` ApiQuery · `0005` subsite · `0006` db-updates · `0007` jobs · `0009` avaliação plugável · `0010` fases · `0011` workplan · `0013` coexistência de temas · `0014` pipeline de assets · `0015` container único · `0016` CI build-only · `0017` deploy por boot. — **Nota de numeração**: `0012` não existe; o pulo `0011`→`0013` é intencional (ADR de cotas descartado na rodada 8 como cerimonial — a mecânica de cotas está documentada no `architecture.md` §14 e no PRD, sem decisão de arquitetura defensável própria).
**Skills** (todas em `.agents/skills/`, 11): `mapas-culturais-controllers` · `mapas-culturais-route-permissions` · `mapas-culturais-module-build` · `mapas-culturais-hooks-dsl` · `mapas-culturais-metadata-eav` · `mapas-culturais-permissions-pcache` · `mapas-culturais-status-lifecycle` · `mapas-culturais-apiquery-dsl` · `mapas-culturais-db-updates-migrations` · `mapas-culturais-vue-component-build` · `mapas-culturais-tdd-builders`.

**Fontes de rastreio** (análises da mesa que fundamentam os rastreios com `arquivo:linha` de cada mecanismo — a referência primária é o `architecture.md`): backend (`r1`,`r3` entidades/hooks, `r4` arquivos, `r6` ApiQuery, `r7` recorrência), DBA (`r1` SQL/EAV/filas, `r7` procedure), frontend (`r1` temas, `r5` SDK/AssetManager), senior (`r1` avaliações, `r3` fases/recurso), devops (`r1` build/deploy), QA (`r1` protocolo/testes), PM (`r1` PRD as-built) — em `.mesa/sessions/202608180036_fedb_mapasculturais-analise-zero-docs-profundas/analyses/`.

**Lacunas conhecidas**: números de linha derivam com o repo; runtime/instâncias externas não verificados (análise estática). Os documentos consolidados de referência estão publicados: `docs/reference/architecture.md` (rastreios por seção), `prd.md`, `jornadas.md` e `gaps.md` (lacunas de documentação).
